@model IEnumerable<DonationPage.Models.DonationEntry>

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()


    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Approved)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AcknowledgementType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Honoree)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comments)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.CheckBoxFor(modelItem => item.Approved)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AcknowledgementType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Honoree)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Comments)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.DonationID }) |
                @Html.ActionLink("Details", "Details", new { id = item.DonationID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.DonationID })
            </td>
        </tr>
    }

    </table>

    <div class="form-group">
        <div class="body">
            <input type="submit" value="Save" class="btn btn-default" />
        </div>
    </div>
}

